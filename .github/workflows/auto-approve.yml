name: Auto approve
on:
  workflow_dispatch:                      # allow manual trigger
  pull_request_target:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: hmarr/auto-approve-action@v4
